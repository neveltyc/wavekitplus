# v1.3.3

## Diff Base

This note is based on the code delta from:

- [v1.3.2](../archive/vcd_analyzer_1_3_2.py)
- [v1.3.3](../archive/vcd_analyzer_1_3_3.py)

## Release Headline

Refine changed-mode and truncation behavior

## Feature / Fix Summary

- Continue polishing changed-mode event handling.
- Tighten text and JSON truncation messaging around streaming search.
- Retain the same sanitized condition-based fixture set.

## CLI Surface Changes

No CLI subcommands were added or removed in this version.

## Code-Level Change Shape

No top-level function additions or removals were detected; this version is primarily an in-place behavior refinement.

## Regression Focus For This Version

The version-specific tests for `v1.3.3` are expected to preserve this tag's historical behavior while exercising its full command surface and key options.

Coverage points:

- `info --json`
- `list --json --filter`
- `dump --begin --end --filter`
- `summary --begin --end --filter`
- `snapshot --at --filter`
- `compare --at --filter`
- `search` primary interface for this version
- `--version`
- `--limit` and `--verbose` on representative streaming/report commands

## Current Validation Result

For the rebuilt local history, tag `v1.3.3` was exported and tested independently.

Result:

- `9` tests executed
- `0` failures
