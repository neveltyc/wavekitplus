# v1.1.4

## Diff Base

This note is based on the code delta from:

- [v1.1.3](../archive/vcd_analyzer_1_1_3.py)
- [v1.1.4](../archive/vcd_analyzer_1_1_4.py)

## Release Headline

Polish search and output consistency

## Feature / Fix Summary

- Tighten text and JSON consistency across the legacy search path.
- Refine parser edge cases while preserving the same feature set.
- Keep the sanitized regression fixtures stable for quick local checks.

## CLI Surface Changes

No CLI subcommands were added or removed in this version.

## Code-Level Change Shape

No top-level function additions or removals were detected; this version is primarily an in-place behavior refinement.

## Regression Focus For This Version

The version-specific tests for `v1.1.4` are expected to preserve this tag's historical behavior while exercising its full command surface and key options.

Coverage points:

- `info --json`
- `list --json --filter`
- `dump --begin --end --filter`
- `summary --begin --end --filter`
- `snapshot --at --filter`
- `compare --at --filter`
- `search` primary interface for this version
- `--version`
- `edges --begin --end --filter`

## Current Validation Result

For the rebuilt local history, tag `v1.1.4` was exported and tested independently.

Result:

- `9` tests executed
- `0` failures
