# v1.2.5

## Diff Base

This note is based on the code delta from:

- [v1.2.4](../archive/vcd_analyzer_1_2_4.py)
- [v1.2.5](../archive/vcd_analyzer_1_2_5.py)

## Release Headline

Add environment-controlled parser limits

## Feature / Fix Summary

- Introduce environment-backed resource limits and filter normalization.
- Start treating hostile or pathological inputs more defensively.
- Keep the test library focused on sanitized smoke coverage, not raw customer traces.

## CLI Surface Changes

No CLI subcommands were added or removed in this version.

## Code-Level Change Shape

Added top-level helpers / commands:
- `_check_time_range`
- `_env_int`
- `_normalize_filter_patterns`

## Regression Focus For This Version

The version-specific tests for `v1.2.5` are expected to preserve this tag's historical behavior while exercising its full command surface and key options.

Coverage points:

- `info --json`
- `list --json --filter`
- `dump --begin --end --filter`
- `summary --begin --end --filter`
- `snapshot --at --filter`
- `compare --at --filter`
- `search` primary interface for this version
- `--version`
- `--limit` and `--verbose` on representative streaming/report commands

## Current Validation Result

For the rebuilt local history, tag `v1.2.5` was exported and tested independently.

Result:

- `9` tests executed
- `0` failures
