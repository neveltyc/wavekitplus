# v1.2.8

## Diff Base

This note is based on the code delta from:

- [v1.2.7](../archive/vcd_analyzer_1_2_7.py)
- [v1.2.8](../archive/vcd_analyzer_1_2_8.py)

## Release Headline

Validate timestamp tokens defensively

## Feature / Fix Summary

- Add dedicated VCD timestamp token validation.
- Reduce the risk of malformed timestamps corrupting downstream analysis.
- Keep the legacy search tests intact against sanitized fixtures.

## CLI Surface Changes

No CLI subcommands were added or removed in this version.

## Code-Level Change Shape

Added top-level helpers / commands:
- `_parse_vcd_timestamp_token`

## Regression Focus For This Version

The version-specific tests for `v1.2.8` are expected to preserve this tag's historical behavior while exercising its full command surface and key options.

Coverage points:

- `info --json`
- `list --json --filter`
- `dump --begin --end --filter`
- `summary --begin --end --filter`
- `snapshot --at --filter`
- `compare --at --filter`
- `search` primary interface for this version
- `--version`
- `--limit` and `--verbose` on representative streaming/report commands

## Current Validation Result

For the rebuilt local history, tag `v1.2.8` was exported and tested independently.

Result:

- `9` tests executed
- `0` failures
