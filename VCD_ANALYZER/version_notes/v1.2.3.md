# v1.2.3

## Diff Base

This note is based on the code delta from:

- [v1.2.2](../archive/vcd_analyzer_1_2_2.py)
- [v1.2.3](../archive/vcd_analyzer_1_2_3.py)

## Release Headline

Refine summary and search payloads

## Feature / Fix Summary

- Tune result payload details while keeping the 1.2 command surface stable.
- Continue cleaning up JSON structures around interval-based search.
- Retain the same unittest coverage points for backwards compatibility.

## CLI Surface Changes

No CLI subcommands were added or removed in this version.

## Code-Level Change Shape

No top-level function additions or removals were detected; this version is primarily an in-place behavior refinement.

## Regression Focus For This Version

The version-specific tests for `v1.2.3` are expected to preserve this tag's historical behavior while exercising its full command surface and key options.

Coverage points:

- `info --json`
- `list --json --filter`
- `dump --begin --end --filter`
- `summary --begin --end --filter`
- `snapshot --at --filter`
- `compare --at --filter`
- `search` primary interface for this version
- `--version`
- `--limit` and `--verbose` on representative streaming/report commands

## Current Validation Result

For the rebuilt local history, tag `v1.2.3` was exported and tested independently.

Result:

- `9` tests executed
- `0` failures
