# v1.2.12

## Diff Base

This note is based on the code delta from:

- [v1.2.11](../archive/vcd_analyzer_1_2_11.py)
- [v1.2.12](../archive/vcd_analyzer_1_2_12.py)

## Release Headline

Capture richer header metadata

## Feature / Fix Summary

- Add date, version, and comment metadata capture from VCD headers.
- Strengthen resource caps around parser state and metadata retention.
- Keep the legacy interval-based search interface as the last 1.2 branch.

## CLI Surface Changes

No CLI subcommands were added or removed in this version.

## Code-Level Change Shape

No top-level function additions or removals were detected; this version is primarily an in-place behavior refinement.

## Regression Focus For This Version

The version-specific tests for `v1.2.12` are expected to preserve this tag's historical behavior while exercising its full command surface and key options.

Coverage points:

- `info --json`
- `list --json --filter`
- `dump --begin --end --filter`
- `summary --begin --end --filter`
- `snapshot --at --filter`
- `compare --at --filter`
- `search` primary interface for this version
- `--version`
- `--limit` and `--verbose` on representative streaming/report commands

## Current Validation Result

For the rebuilt local history, tag `v1.2.12` was exported and tested independently.

Result:

- `9` tests executed
- `0` failures
