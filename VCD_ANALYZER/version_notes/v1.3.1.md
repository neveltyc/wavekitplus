# v1.3.1

## Diff Base

This note is based on the code delta from:

- [v1.3.0](../archive/vcd_analyzer_1_3_0.py)
- [v1.3.1](../archive/vcd_analyzer_1_3_1.py)

## Release Headline

Add truncation accounting for streaming commands

## Feature / Fix Summary

- Add lower-bound counting helpers for truncated dump and search results.
- Improve JSON and text reporting when scans stop after the first hidden record.
- Keep the sanitized condition-search tests aligned with the new result metadata.

## CLI Surface Changes

No CLI subcommands were added or removed in this version.

## Code-Level Change Shape

Added top-level helpers / commands:
- `_count_label`
- `_total_json_fields`
- `_trunc_line_lower_bound`

## Regression Focus For This Version

The version-specific tests for `v1.3.1` are expected to preserve this tag's historical behavior while exercising its full command surface and key options.

Coverage points:

- `info --json`
- `list --json --filter`
- `dump --begin --end --filter`
- `summary --begin --end --filter`
- `snapshot --at --filter`
- `compare --at --filter`
- `search` primary interface for this version
- `--version`
- `--limit` and `--verbose` on representative streaming/report commands

## Current Validation Result

For the rebuilt local history, tag `v1.3.1` was exported and tested independently.

Result:

- `9` tests executed
- `0` failures
