# v1.3.0

## Diff Base

This note is based on the code delta from:

- [v1.2.12](../archive/vcd_analyzer_1_2_12.py)
- [v1.3.0](../archive/vcd_analyzer_1_3_0.py)

## Release Headline

Redesign search around conditions and observations

## Feature / Fix Summary

- Replace legacy value search with condition-based search, show-signals, and changed-signal modes.
- Keep the rest of the waveform inspection CLI familiar while modernizing search semantics.
- Introduce a dedicated sanitized search fixture for the new interface.

## CLI Surface Changes

No CLI subcommands were added or removed in this version.

## Code-Level Change Shape

Added top-level helpers / commands:
- `_condition_label`
- `_condition_match`
- `_condition_result_text`
- `_conditions_hold`
- `_has_unknown`
- `_parse_conditions`
- `_resolve_conditions`
- `_resolve_one_signal`
- `_resolve_show_sids`
- `_search_end_time`
- `_show_values`
- `_values_text`

## Regression Focus For This Version

The version-specific tests for `v1.3.0` are expected to preserve this tag's historical behavior while exercising its full command surface and key options.

Coverage points:

- `info --json`
- `list --json --filter`
- `dump --begin --end --filter`
- `summary --begin --end --filter`
- `snapshot --at --filter`
- `compare --at --filter`
- `search` primary interface for this version
- `--version`
- `--limit` and `--verbose` on representative streaming/report commands

## Current Validation Result

For the rebuilt local history, tag `v1.3.0` was exported and tested independently.

Result:

- `9` tests executed
- `0` failures
