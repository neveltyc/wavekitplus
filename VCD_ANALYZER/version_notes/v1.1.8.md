# v1.1.8

## Diff Base

This note is based on the code delta from:

- [v1.1.7](../archive/vcd_analyzer_1_1_7.py)
- [v1.1.8](../archive/vcd_analyzer_1_1_8.py)

## Release Headline

Switch legacy search to interval reporting

## Feature / Fix Summary

- Upgrade legacy value search from point hits to interval-style results.
- Add explicit target parsing and width-aware value matching helpers.
- Keep edge analysis available while modernizing the search output payload.

## CLI Surface Changes

No CLI subcommands were added or removed in this version.

## Code-Level Change Shape

Added top-level helpers / commands:
- `_parse_target_value`
- `_selected_sids`
- `_signal_value_intervals`
- `_value_matches`

## Regression Focus For This Version

The version-specific tests for `v1.1.8` are expected to preserve this tag's historical behavior while exercising its full command surface and key options.

Coverage points:

- `info --json`
- `list --json --filter`
- `dump --begin --end --filter`
- `summary --begin --end --filter`
- `snapshot --at --filter`
- `compare --at --filter`
- `search` primary interface for this version
- `--version`
- `edges --begin --end --filter`

## Current Validation Result

For the rebuilt local history, tag `v1.1.8` was exported and tested independently.

Result:

- `9` tests executed
- `0` failures
