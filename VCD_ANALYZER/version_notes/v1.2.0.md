# v1.2.0

## Diff Base

This note is based on the code delta from:

- [v1.1.8](../archive/vcd_analyzer_1_1_8.py)
- [v1.2.0](../archive/vcd_analyzer_1_2_0.py)

## Release Headline

Remove edges command and add shared output helpers

## Feature / Fix Summary

- Drop the dedicated edges command from the public CLI.
- Introduce shared JSON, clipping, and summary row helpers.
- Retain the legacy interval-based search interface for this major line.

## CLI Surface Changes

Removed subcommands:
- `edges` removed after [v1.1.8.py](../archive/vcd_analyzer_1_1_8.py)

## Code-Level Change Shape

Added top-level helpers / commands:
- `_add_common`
- `_clip`
- `_event_groups`
- `_fmt_maybe`
- `_json`
- `_limit`
- `_public_row`
- `_summary_rows`
- `_trunc_line`
Removed top-level helpers / commands:
- `cmd_edges`

## Regression Focus For This Version

The version-specific tests for `v1.2.0` are expected to preserve this tag's historical behavior while exercising its full command surface and key options.

Coverage points:

- `info --json`
- `list --json --filter`
- `dump --begin --end --filter`
- `summary --begin --end --filter`
- `snapshot --at --filter`
- `compare --at --filter`
- `search` primary interface for this version
- `--version`
- `--limit` and `--verbose` on representative streaming/report commands

## Current Validation Result

For the rebuilt local history, tag `v1.2.0` was exported and tested independently.

Result:

- `9` tests executed
- `0` failures
