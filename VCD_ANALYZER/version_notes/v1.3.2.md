# v1.3.2

## Diff Base

This note is based on the code delta from:

- [v1.3.1](../archive/vcd_analyzer_1_3_1.py)
- [v1.3.2](../archive/vcd_analyzer_1_3_2.py)

## Release Headline

Preserve begin-boundary edges in changed mode

## Feature / Fix Summary

- Add a pre-begin snapshot helper so changed-mode queries can see edges at the window boundary.
- Keep the condition/show workflow intact while tightening event semantics.
- Update the search fixtures and tests around changed-mode behavior.

## CLI Surface Changes

No CLI subcommands were added or removed in this version.

## Code-Level Change Shape

Added top-level helpers / commands:
- `_build_snapshot_before`

## Regression Focus For This Version

The version-specific tests for `v1.3.2` are expected to preserve this tag's historical behavior while exercising its full command surface and key options.

Coverage points:

- `info --json`
- `list --json --filter`
- `dump --begin --end --filter`
- `summary --begin --end --filter`
- `snapshot --at --filter`
- `compare --at --filter`
- `search` primary interface for this version
- `--version`
- `--limit` and `--verbose` on representative streaming/report commands

## Current Validation Result

For the rebuilt local history, tag `v1.3.2` was exported and tested independently.

Result:

- `9` tests executed
- `0` failures
