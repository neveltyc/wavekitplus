# v1.1.1

## Diff Base

This note is based on the code delta from:

- [v1.1.0](../archive/vcd_analyzer_1_1_0.py)
- [v1.1.1](../archive/vcd_analyzer_1_1_1.py)

## Release Headline

Harden multiline token cleanup

## Feature / Fix Summary

- Tighten cleanup for split and multiline data tokens.
- Stabilize parsing around malformed or oddly wrapped value-change text.
- Refresh the test expectations while keeping the same public command set.

## CLI Surface Changes

No CLI subcommands were added or removed in this version.

## Code-Level Change Shape

No top-level function additions or removals were detected; this version is primarily an in-place behavior refinement.

## Regression Focus For This Version

The version-specific tests for `v1.1.1` are expected to preserve this tag's historical behavior while exercising its full command surface and key options.

Coverage points:

- `info --json`
- `list --json --filter`
- `dump --begin --end --filter`
- `summary --begin --end --filter`
- `snapshot --at --filter`
- `compare --at --filter`
- `search` primary interface for this version
- `--version`
- `edges --begin --end --filter`

## Current Validation Result

For the rebuilt local history, tag `v1.1.1` was exported and tested independently.

Result:

- `9` tests executed
- `0` failures
