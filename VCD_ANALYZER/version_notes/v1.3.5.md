# v1.3.5

## Diff Base

This note is based on the code delta from:

- [v1.3.4](../archive/vcd_analyzer_1_3_4.py)
- [v1.3.5](../archive/vcd_analyzer_1_3_5.py)

## Release Headline

Remove obsolete search helper

## Feature / Fix Summary

- Delete the dead interval helper left behind by the search redesign.
- Keep the user-facing command set stable while simplifying internals.
- Carry forward the sanitized condition-search regression cases.

## CLI Surface Changes

No CLI subcommands were added or removed in this version.

## Code-Level Change Shape

Removed top-level helpers / commands:
- `_signal_value_intervals`

## Regression Focus For This Version

The version-specific tests for `v1.3.5` are expected to preserve this tag's historical behavior while exercising its full command surface and key options.

Coverage points:

- `info --json`
- `list --json --filter`
- `dump --begin --end --filter`
- `summary --begin --end --filter`
- `snapshot --at --filter`
- `compare --at --filter`
- `search` primary interface for this version
- `--version`
- `--limit` and `--verbose` on representative streaming/report commands

## Current Validation Result

For the rebuilt local history, tag `v1.3.5` was exported and tested independently.

Result:

- `9` tests executed
- `0` failures
