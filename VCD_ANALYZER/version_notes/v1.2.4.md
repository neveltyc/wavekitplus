# v1.2.4

## Diff Base

This note is based on the code delta from:

- [v1.2.3](../archive/vcd_analyzer_1_2_3.py)
- [v1.2.4](../archive/vcd_analyzer_1_2_4.py)

## Release Headline

Harden time parsing and CLI guards

## Feature / Fix Summary

- Keep the earlier 1.2.4 snapshot because it stays closest to both 1.2.3 and 1.2.5.
- Tighten time parsing, overflow handling, and general CLI validation.
- Preserve the legacy search interface and sanitized regression assets.

## CLI Surface Changes

No CLI subcommands were added or removed in this version.

## Code-Level Change Shape

No top-level function additions or removals were detected; this version is primarily an in-place behavior refinement.

## Regression Focus For This Version

The version-specific tests for `v1.2.4` are expected to preserve this tag's historical behavior while exercising its full command surface and key options.

Coverage points:

- `info --json`
- `list --json --filter`
- `dump --begin --end --filter`
- `summary --begin --end --filter`
- `snapshot --at --filter`
- `compare --at --filter`
- `search` primary interface for this version
- `--version`
- `--limit` and `--verbose` on representative streaming/report commands

## Current Validation Result

For the rebuilt local history, tag `v1.2.4` was exported and tested independently.

Result:

- `9` tests executed
- `0` failures
