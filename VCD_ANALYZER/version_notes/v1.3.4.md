# v1.3.4

## Diff Base

This note is based on the code delta from:

- [v1.3.3](../archive/vcd_analyzer_1_3_3.py)
- [v1.3.4](../archive/vcd_analyzer_1_3_4.py)

## Release Headline

Support width-aware 4-state matching

## Feature / Fix Summary

- Add explicit helpers for 4-state bit detection and left-extension.
- Make x/z pattern matching width-aware for condition search.
- Extend the test suite around mixed known/unknown vector comparisons.

## CLI Surface Changes

No CLI subcommands were added or removed in this version.

## Code-Level Change Shape

Added top-level helpers / commands:
- `_is_4state_bits`
- `_left_extend_bits`

## Regression Focus For This Version

The version-specific tests for `v1.3.4` are expected to preserve this tag's historical behavior while exercising its full command surface and key options.

Coverage points:

- `info --json`
- `list --json --filter`
- `dump --begin --end --filter`
- `summary --begin --end --filter`
- `snapshot --at --filter`
- `compare --at --filter`
- `search` primary interface for this version
- `--version`
- `--limit` and `--verbose` on representative streaming/report commands

## Current Validation Result

For the rebuilt local history, tag `v1.3.4` was exported and tested independently.

Result:

- `9` tests executed
- `0` failures
