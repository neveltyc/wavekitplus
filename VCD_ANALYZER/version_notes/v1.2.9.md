# v1.2.9

## Diff Base

This note is based on the code delta from:

- [v1.2.8](../archive/vcd_analyzer_1_2_8.py)
- [v1.2.9](../archive/vcd_analyzer_1_2_9.py)

## Release Headline

Cap integer parsing in headers

## Feature / Fix Summary

- Introduce bounded integer parsing for widths and bit indices.
- Harden header parsing against split or oversized numeric fields.
- Retain the same regression-friendly fixture layout.

## CLI Surface Changes

No CLI subcommands were added or removed in this version.

## Code-Level Change Shape

Added top-level helpers / commands:
- `_safe_int_digits`

## Regression Focus For This Version

The version-specific tests for `v1.2.9` are expected to preserve this tag's historical behavior while exercising its full command surface and key options.

Coverage points:

- `info --json`
- `list --json --filter`
- `dump --begin --end --filter`
- `summary --begin --end --filter`
- `snapshot --at --filter`
- `compare --at --filter`
- `search` primary interface for this version
- `--version`
- `--limit` and `--verbose` on representative streaming/report commands

## Current Validation Result

For the rebuilt local history, tag `v1.2.9` was exported and tested independently.

Result:

- `9` tests executed
- `0` failures
