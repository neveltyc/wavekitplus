# v1.3.6

## Diff Base

This note is based on the code delta from:

- [v1.3.5](../archive/vcd_analyzer_1_3_5.py)
- [v1.3.6](../archive/vcd_analyzer_1_3_6.py)

## Release Headline

Clamp malformed over-wide logic values

## Feature / Fix Summary

- Degrade malformed over-wide logic dumps to explicit unknowns instead of plausible numeric values.
- Retain the 1.3 search contract while improving corrupted-input behavior.
- Keep the sanitized regression cases aligned with the over-wide handling rules.

## CLI Surface Changes

No CLI subcommands were added or removed in this version.

## Code-Level Change Shape

Added top-level helpers / commands:
- `_clamp_overwide_logic_value`

## Regression Focus For This Version

The version-specific tests for `v1.3.6` are expected to preserve this tag's historical behavior while exercising its full command surface and key options.

Coverage points:

- `info --json`
- `list --json --filter`
- `dump --begin --end --filter`
- `summary --begin --end --filter`
- `snapshot --at --filter`
- `compare --at --filter`
- `search` primary interface for this version
- `--version`
- `--limit` and `--verbose` on representative streaming/report commands

## Current Validation Result

For the rebuilt local history, tag `v1.3.6` was exported and tested independently.

Result:

- `9` tests executed
- `0` failures
