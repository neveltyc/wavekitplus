# v1.2.6

## Diff Base

This note is based on the code delta from:

- [v1.2.5](../archive/vcd_analyzer_1_2_5.py)
- [v1.2.6](../archive/vcd_analyzer_1_2_6.py)

## Release Headline

Tighten regex and malformed-input handling

## Feature / Fix Summary

- Refine real-value parsing and malformed token handling.
- Continue the defensive-parser work without changing the legacy CLI contract.
- Retain the same fixture set and interval-style search tests.

## CLI Surface Changes

No CLI subcommands were added or removed in this version.

## Code-Level Change Shape

No top-level function additions or removals were detected; this version is primarily an in-place behavior refinement.

## Regression Focus For This Version

The version-specific tests for `v1.2.6` are expected to preserve this tag's historical behavior while exercising its full command surface and key options.

Coverage points:

- `info --json`
- `list --json --filter`
- `dump --begin --end --filter`
- `summary --begin --end --filter`
- `snapshot --at --filter`
- `compare --at --filter`
- `search` primary interface for this version
- `--version`
- `--limit` and `--verbose` on representative streaming/report commands

## Current Validation Result

For the rebuilt local history, tag `v1.2.6` was exported and tested independently.

Result:

- `9` tests executed
- `0` failures
