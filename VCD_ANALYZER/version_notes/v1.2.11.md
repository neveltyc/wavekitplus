# v1.2.11

## Diff Base

This note is based on the code delta from:

- [v1.2.10](../archive/vcd_analyzer_1_2_10.py)
- [v1.2.11](../archive/vcd_analyzer_1_2_11.py)

## Release Headline

Improve malformed token recovery

## Feature / Fix Summary

- Refine recovery from malformed data tokens and skipped sections.
- Continue tightening tolerant-reader behavior for imperfect traces.
- Maintain the same repository structure and smoke tests.

## CLI Surface Changes

No CLI subcommands were added or removed in this version.

## Code-Level Change Shape

No top-level function additions or removals were detected; this version is primarily an in-place behavior refinement.

## Regression Focus For This Version

The version-specific tests for `v1.2.11` are expected to preserve this tag's historical behavior while exercising its full command surface and key options.

Coverage points:

- `info --json`
- `list --json --filter`
- `dump --begin --end --filter`
- `summary --begin --end --filter`
- `snapshot --at --filter`
- `compare --at --filter`
- `search` primary interface for this version
- `--version`
- `--limit` and `--verbose` on representative streaming/report commands

## Current Validation Result

For the rebuilt local history, tag `v1.2.11` was exported and tested independently.

Result:

- `9` tests executed
- `0` failures
