# v1.3.7

## Diff Base

This note is based on the code delta from:

- [v1.3.6](../archive/vcd_analyzer_1_3_6.py)
- [v1.3.7](../archive/vcd_analyzer_1_3_7.py)

## Release Headline

Fix literal bus-range globs and escaped-scope reporting

## Feature / Fix Summary

- Replace fnmatch-based wildcard matching with a literal-bracket glob-lite matcher.
- Use declaration-time scope metadata when reporting scopes for escaped identifiers.
- Keep the latest sanitized condition-search regression suite in place.

## CLI Surface Changes

No CLI subcommands were added or removed in this version.

## Code-Level Change Shape

Added top-level helpers / commands:
- `_glob_lite_regex`

## Regression Focus For This Version

The version-specific tests for `v1.3.7` are expected to preserve this tag's historical behavior while exercising its full command surface and key options.

Coverage points:

- `info --json`
- `list --json --filter`
- `dump --begin --end --filter`
- `summary --begin --end --filter`
- `snapshot --at --filter`
- `compare --at --filter`
- `search` primary interface for this version
- `--version`
- `--limit` and `--verbose` on representative streaming/report commands
- literal bus-range glob regression (`*data[7:0]`)
- escaped identifier scope regression

## Current Validation Result

For the rebuilt local history, tag `v1.3.7` was exported and tested independently.

Result:

- `9` tests executed
- `0` failures
