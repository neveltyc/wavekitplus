# v1.2.7

## Diff Base

This note is based on the code delta from:

- [v1.2.6](../archive/vcd_analyzer_1_2_6.py)
- [v1.2.7](../archive/vcd_analyzer_1_2_7.py)

## Release Headline

Refine safety bounds and filtering

## Feature / Fix Summary

- Polish parser bounds and filtering safety checks.
- Keep the external command surface stable for legacy users.
- Carry forward the same sanitized VCD stimuli and unittest structure.

## CLI Surface Changes

No CLI subcommands were added or removed in this version.

## Code-Level Change Shape

No top-level function additions or removals were detected; this version is primarily an in-place behavior refinement.

## Regression Focus For This Version

The version-specific tests for `v1.2.7` are expected to preserve this tag's historical behavior while exercising its full command surface and key options.

Coverage points:

- `info --json`
- `list --json --filter`
- `dump --begin --end --filter`
- `summary --begin --end --filter`
- `snapshot --at --filter`
- `compare --at --filter`
- `search` primary interface for this version
- `--version`
- `--limit` and `--verbose` on representative streaming/report commands

## Current Validation Result

For the rebuilt local history, tag `v1.2.7` was exported and tested independently.

Result:

- `9` tests executed
- `0` failures
