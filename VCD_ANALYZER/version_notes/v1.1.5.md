# v1.1.5

## Diff Base

This note is based on the code delta from:

- [v1.1.4](../archive/vcd_analyzer_1_1_4.py)
- [v1.1.5](../archive/vcd_analyzer_1_1_5.py)

## Release Headline

Simplify token scanning paths

## Feature / Fix Summary

- Remove the extra cleanup helper and consolidate data token traversal.
- Clean up internal parser flow without changing command names.
- Retain the existing smoke tests for the legacy command surface.

## CLI Surface Changes

No CLI subcommands were added or removed in this version.

## Code-Level Change Shape

No top-level function additions or removals were detected; this version is primarily an in-place behavior refinement.

## Regression Focus For This Version

The version-specific tests for `v1.1.5` are expected to preserve this tag's historical behavior while exercising its full command surface and key options.

Coverage points:

- `info --json`
- `list --json --filter`
- `dump --begin --end --filter`
- `summary --begin --end --filter`
- `snapshot --at --filter`
- `compare --at --filter`
- `search` primary interface for this version
- `--version`
- `edges --begin --end --filter`

## Current Validation Result

For the rebuilt local history, tag `v1.1.5` was exported and tested independently.

Result:

- `9` tests executed
- `0` failures
