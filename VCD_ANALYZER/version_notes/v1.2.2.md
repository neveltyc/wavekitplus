# v1.2.2

## Diff Base

This note is based on the code delta from:

- [v1.2.1](../archive/vcd_analyzer_1_2_1.py)
- [v1.2.2](../archive/vcd_analyzer_1_2_2.py)

## Release Headline

Expand time metadata fields

## Feature / Fix Summary

- Add richer begin/end time metadata helpers for JSON-oriented outputs.
- Keep the legacy search and summary workflow intact.
- Refresh the historical test fixture set with the restored archive snapshot.

## CLI Surface Changes

No CLI subcommands were added or removed in this version.

## Code-Level Change Shape

Added top-level helpers / commands:
- `_time_pair`

## Regression Focus For This Version

The version-specific tests for `v1.2.2` are expected to preserve this tag's historical behavior while exercising its full command surface and key options.

Coverage points:

- `info --json`
- `list --json --filter`
- `dump --begin --end --filter`
- `summary --begin --end --filter`
- `snapshot --at --filter`
- `compare --at --filter`
- `search` primary interface for this version
- `--version`
- `--limit` and `--verbose` on representative streaming/report commands

## Current Validation Result

For the rebuilt local history, tag `v1.2.2` was exported and tested independently.

Result:

- `9` tests executed
- `0` failures
